#include <bits/stdc++.h>
#define int long long
#define endl "\n"
using namespace std;
//const int MAXN=;
//int maxn=INT_MIN;
//int minn=INT_MAX;
int n,m,a[1000005];
signed main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> n >> m;
    for(int i=1; i<=n; i++) cin >> a[i];
    while(m--) {
        int v,op,p,c;
        cin >> v >> op >> p;
        if(op==1) {
            cin >> c;
            
        }
        else {
            
        }
    }
    return 0;
}